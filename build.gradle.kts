plugins {
    id("cloudstreamplugin")
}

cloudstream {
    language = "ar"
    description = "مصدر مخصص يحتوي على مكتبة ضخمة من الأنمي العربي."
    authors = listOf("YourName")
    status = 1
    tvTypes = listOf(TvType.Anime)
}
